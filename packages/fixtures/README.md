# Ceeline Fixtures

This package contains canonical example inputs and envelope outputs for Ceeline
v1 tests.

Suggested first fixture groups:

- `canonical/`: readable source content classified by surface
- `envelopes/`: expected Ceeline v1 envelopes
- `renders/`: expected internal and user-facing render outputs
